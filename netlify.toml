[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "API_PORT,APP_URL"
  SECRETS_SCAN_OMIT_PATHS = "prisma/dev.db"
